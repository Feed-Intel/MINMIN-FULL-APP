name: Deploy Customer Frontend

on:
  push:
    branches: [develop, main]
    paths:
      - 'customerFE/**'
      - '.github/workflows/deploy-fe-customer.yml'

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: customerFE/package-lock.json

      - name: Install dependencies
        working-directory: customerFE
        run: npm ci

      - name: Build
        working-directory: customerFE
        run: npm run build

      - name: Archive build
        run: |
          cd customerFE && zip -r ../customerFE.zip dist

      - name: Deploy to Amplify
        uses: aws-amplify/amplify-hosting-deploy@v2
        with:
          app-id: ${{ secrets.AMPLIFY_APP_ID_CUSTOMER }}
          branch: ${{ vars.AMPLIFY_BRANCH }}
          artifact: customerFE.zip
